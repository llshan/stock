"""Shared utilities package.

This file makes the `utils` directory a Python package so that
imports like `from utils.logging_utils import setup_logging` work
when running tools from the project root.
"""
